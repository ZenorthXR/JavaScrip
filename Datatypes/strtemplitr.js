// Concatenation with template Literals
console.log("\n********Connatenation with template literals*******\n");

let str1 = "JavaScript";
let str2 = "Fun";

console.log(`I am writing code in ${str1}`);
console.log(`Formatting in ${str1} is ${str2}!`);

// Expressions in template literals
console.log("\n********Expression in template literal********\n")

let bool1 = true;
console.log(`1 + 1 is ${1 + 1}`);
console.log(`The opposite of true is ${!bool1}`);
//ในเครื่องหมาย ${สามารถใส่เป็นฟังชันก์ ได้ ใช้ +-*/ + ได้หมด}